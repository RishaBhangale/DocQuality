BACKEND SETUP

cd "path\DocQuality\backend"
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload

FRONTEND SETUP

cd "path\DocQuality\frontend"
npm install
npm run dev